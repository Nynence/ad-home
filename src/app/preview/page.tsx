import DevicePreviewCanvas from "@/components/DevicePreviewCanvas";

export const metadata = {
  title: "Preview | A&D Hero",
};

export default function PreviewPage() {
  return <DevicePreviewCanvas />;
}
